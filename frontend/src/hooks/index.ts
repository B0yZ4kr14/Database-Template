/**
 * Hooks Index
 * ===========
 * Exportação centralizada dos hooks customizados
 */

// Hooks para API (disponíveis para uso futuro, atualmente usando fetch direto)
export { useApi, useGet, usePost } from './useApi';

// Hook para mensagens temporárias
export { useMessage } from './useMessage';
